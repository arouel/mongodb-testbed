package core;

/**
 * An alternative to {@link Void} that is actually once inhabited (whereas <code>Void</code> is inhabited by <code>null</code>, which causes NPEs).
 */
public enum Unit {
    VALUE;
}
